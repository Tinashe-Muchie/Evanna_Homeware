import { 
  HashRouter as Router, 
  Routes, 
  Route } from 'react-router-dom';
import {
  HEAD
} from './Components/index';

export const ROUTING = () => {
  return (
    <Router>
      <Routes>
        <Route path
      </Routes>
    </Router>
  );
}